# The vendored libs/ag-ui-client depends on @modelcontextprotocol/ext-apps, which requires
# @modelcontextprotocol/sdk ^1.29, while Angular CLI 21 bundles sdk 1.26. Relax peer-dependency
# resolution so `npm install` succeeds without the --legacy-peer-deps flag.
legacy-peer-deps=true
